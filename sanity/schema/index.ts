import { categoryType } from './category';
import { projectType } from './project';
import { blogCategoryType } from './blogCategory';
import { blogArticleType } from './blogArticle';

export const schemaTypes = [categoryType, projectType, blogCategoryType, blogArticleType];
